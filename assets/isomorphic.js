// isomorphic.js - Classic iso grid removed; functionality consolidated into screen-perform.js
// Stub kept for backward compat (SL.isomorphic namespace referenced elsewhere)
(function() {
  'use strict';
  var SL = window.SynthLab;
  if (!SL) { return; }
  SL.isomorphic = {
    buildIso: function() { /* no-op */ }
  };
})();
